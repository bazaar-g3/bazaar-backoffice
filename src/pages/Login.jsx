// TODO: implementar formulario de login
// Solo admins pueden acceder al backoffice
// Al hacer login exitoso guardar JWT en localStorage y redirigir a /dashboard

/**
 * Componente de la página de inicio de sesión del backoffice. Permite
 * autenticarse a usuarios con rol de administrador. Ante un login exitoso,
 * almacena el JWT en localStorage y redirige al usuario hacia el dashboard.
 *
 * @returns {JSX.Element} Componente renderizado con el formulario de login.
 */
export default function Login() {
  return <div>Login — pendiente de implementar</div>
}
