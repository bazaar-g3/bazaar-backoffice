// TODO: implementar moderación de contenido
// - Listar productos reportados
// - Dar de baja productos que rompen las reglas (DELETE /products/{id})

/**
 * Componente de la página de moderación de productos. Permite listar los
 * productos reportados por incumplimiento de reglas y darlos de baja
 * mediante la API correspondiente (DELETE /products/{id}).
 *
 * @returns {JSX.Element} Componente renderizado con la lista de productos reportados y acciones de moderación.
 */
export default function Products() {
  return <div>Moderación de productos — pendiente de implementar</div>
}
