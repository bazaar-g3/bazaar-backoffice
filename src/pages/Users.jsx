// TODO: implementar gestión de usuarios
// - Listar todos los usuarios (GET /users/)
// - Bloquear cuenta (PATCH /users/{id}/block)
// - Desbloquear cuenta (PATCH /users/{id}/unblock)

/**
 * Componente de la página de gestión de usuarios. Permite listar todos los
 * usuarios registrados en el sistema y realizar acciones administrativas como
 * bloquear o desbloquear cuentas a través de la API de usuarios.
 *
 * @returns {JSX.Element} Componente renderizado con la tabla de usuarios y las acciones de gestión.
 */
export default function Users() {
  return <div>Gestión de usuarios — pendiente de implementar</div>
}
