// TODO: implementar visualización de órdenes para soporte técnico
// - Listar todas las órdenes (GET /orders/admin/all)
// - Ver detalle de orden y estado del pago
// - Filtrar por fecha, estado, usuario

/**
 * Componente de la página de gestión de órdenes. Permite al equipo de soporte
 * técnico listar todas las órdenes del sistema, consultar el detalle y estado
 * de pago de cada una, y aplicar filtros por fecha, estado o usuario.
 *
 * @returns {JSX.Element} Componente renderizado con la tabla de órdenes y sus filtros.
 */
export default function Orders() {
  return <div>Órdenes — pendiente de implementar</div>
}
